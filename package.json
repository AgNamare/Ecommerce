{
  "name": "mern-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node ./server/src/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}
